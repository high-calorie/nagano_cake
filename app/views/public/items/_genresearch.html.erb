<table border = '1' class = "text-center"  width="70%">
  <tr>
    <td>ジャンル検索</td>
  </tr>
  <% genres.each do |genre| %>
  <tr>
    <td>
    <%= link_to genre.genre_name, admin_genres_path(genre_id: genre.id), class: "text-secondary" %>
    </td>
  </tr>
  <% end %>
</table>